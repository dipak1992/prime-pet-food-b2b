import { SectionCard } from "@/components/ui/SectionCard";

export default function AccountPage() {
  return (
    <SectionCard title="Account settings" description="Manage business details, addresses, and communication preferences.">
      <p className="text-sm text-[#4b5563]">Starter account settings screen is ready for profile and address forms.</p>
    </SectionCard>
  );
}
